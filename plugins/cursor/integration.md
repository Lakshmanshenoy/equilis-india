# Cursor Integration

Use the same skill files and templates as VS Code.

Responsibilities:
- Load skill prompt modules
- Pass validated input JSON
- Render the final report with no extra analysis logic in the plugin
