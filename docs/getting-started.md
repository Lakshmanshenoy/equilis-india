# Getting Started

1. Create an input JSON file.
2. Validate it against the schema.
3. Run the CLI with the input file.
4. Render the markdown output to PDF if needed.

## Output Location

Save final PDFs in `~/Downloads` by default.
